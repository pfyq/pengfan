server:
  port: 8088
spring:
  application:
    name: config-client-nacos
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #指定Nacos配置中心的地址
      config:
        server-addr: 127.0.0.1:8848 #指定Nacos服务配置中心地址
        file-extension: yaml #指定yaml格式的配置（原先GitHub上的配置文件）